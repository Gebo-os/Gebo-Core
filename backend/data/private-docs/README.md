# Drop owner private documents here for Gebo learning ingestion.
# Supported: .md, .txt, .json, .csv (max 256 KB each).
# Run: POST /knowledge/collect or .\scripts\gebo-knowledge-collect.ps1
# Documents are stored as memory_type=private_doc — local only, never uploaded.
